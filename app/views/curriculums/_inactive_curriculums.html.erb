<% unless @inactive_curriculums.empty? %>
  <div class="panel clearfix">
    <h1>Inactive Curriculums</h1>
    <ul class="side-nav">
      <% @inactive_curriculums.each do |curriculum| %>
        <li><%= link_to curriculum.name, curriculum_path(curriculum) %></li>
      <% end %>
    </ul>
  </div>
<% end %>