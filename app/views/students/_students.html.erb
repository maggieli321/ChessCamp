<h3><%= status %> Students</h3>
<% if students.size == 0 %>
  Sorry, there are no students to be displayed.
<% else %>

<table class="table">
  <tr>
    <th width="200">Name</th>
    <th width="100">Age</th>
    <th width="100">Rating</th>
    <th width="100">Active</th>
    <th></th>
    <th></th>
  </tr>

<% students.each do |student| %>
  <tr>
    <td><%= link_to student.name, student_path(student) %></td>
    <td align="center"><%= student.age %></td>
    <td align="center">  <%= student.rating.zero? ? "Unrated" : student.rating %></td>
    <td align="center"><%= student.active ? "Yes" : "No" %></td>


  </tr>
<% end %>
</table>
<% end %>

<!-- Add the pagination using will_paginate gem -->
<span class="paginate">
<%= will_paginate students, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
</span>